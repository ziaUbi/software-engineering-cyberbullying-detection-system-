{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Raw Session Schema",
  "description": "Schema validation for the Raw Session structure input to the Preparation System",
  "type": "object",
  "required": [
    "uuid",
    "tweet",
    "audio",
    "events",
    "label"
  ],
  "properties": {
    "uuid": {
      "type": "string",
      "description": "Unique identifier for the session",
      "format": "uuid"
    },
    "tweet": {
      "type": "string",
      "description": "The text content of the tweet"
    },
    "audio": {
      "type": "string",
      "description": "File path to the associated audio file"
    },
    "events": {
    "type": "array",
    "items": {
        "type": "string"
    }
    }, 
    "label": {
      "type": ["string", "null"],
      "description": "Classification label (e.g., cyberbullying). Can be null if in production phase."
    }
  }
}